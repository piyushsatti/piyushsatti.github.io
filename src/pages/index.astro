---
import BaseLayout from "../layouts/BaseLayout.astro";
import PostCard from "../components/PostCard.astro";
import { getCollection } from "astro:content";

const base = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : import.meta.env.BASE_URL + "/";

// Blog posts (theme expects 'blog' collection)
const posts = (await getCollection("blog")).slice(0, 2);

// Projects (custom collection)
const projects = (await getCollection("projects")).slice(0, 2);
---

<BaseLayout title="Home" description="Welcome to my personal website">
  <div class="index-content framed">
    <h1 id="hello-there">Piyush Satti</h1>
    <p>
      I am a Software Developer with a strong foundation in computer science,
      hands-on freelance experience, and published research in algorithm
      development.
    </p>
    <p>
      Graduated with <b
        >M.Sc. in Applied Computer Science at Concordia University (Montreal,
        Canada)</b
      >. I bring a trifecta: academic rigor, engineering experience, and
      entrepreneurial mindset.
    </p>

    <p>
      <a href={`${base}about`}>About Me</a> ::
      <a href={`${base}blog`}>Blog</a> ::
      <a href={`${base}resume.pdf`} target="_blank" rel="noopener noreferrer"
        >Resume</a
      >
    </p>
  </div>

  <div class="posts">
    <h2 class="posts-title">Featured Projects</h2>
    <p>
      {
        projects.map((p) => (
          <p>
            <a href={`${base}projects/${p.id}/`}>{p.data.title}</a>
            <span> — {p.data.summary}</span>
          </p>
        ))
      }
    </p>
  </div>

  <div class="posts">
    <h2 class="posts-title">Latest Posts</h2>
    <p>
      {
        posts.map((p) => (
          <p>
            <a href={`${base}projects/${p.id}/`}>{p.data.title}</a>
            <span> — {p.data.description}</span>
          </p>
        ))
      }
    </p>
  </div>

</BaseLayout>
